MONGODB_URI= (MongoDB Atlas se)
JWT_SECRET= (generate karna hai)
ENCRYPTION_KEY= (generate karna hai)
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_PHONE=
EMAIL_HOST=smtp.gmail.com
EMAIL_USER=
EMAIL_PASS=
const envPath = path.join(projectRoot, '.env');
if (!fs.existsSync(envPath)) {
  console.warn('⚠️ .env file missing – using process.env');
}